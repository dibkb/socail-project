const styles: Record<string, React.CSSProperties> = {
  container: {
    minWidth: 369,
    display: "flex",
    flexDirection: "column",
    alignItems: "start",
    padding: "1rem 1.5rem",
    borderRadius: ".5rem",
    background: "#fff",
  },
};
export default styles;
